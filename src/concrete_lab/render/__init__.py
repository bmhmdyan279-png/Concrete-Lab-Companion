"""Rendering layer: display-only output producers.

The Excel renderer consumes a pure-data :mod:`model` assembled from
domain results and never computes anything itself — the separation
contract enforced by the structural QA tier.
"""
