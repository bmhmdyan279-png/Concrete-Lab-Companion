# تاریخچه تغییرات (Changelog)

همه تغییرات مهم این پروژه در این فایل ثبت می‌شود.

فرمت بر اساس [Keep a Changelog](https://keepachangelog.com/fa/1.0.0/) و نسخه‌گذاری بر اساس [Semantic Versioning](https://semver.org/lang/fa/) است.

---

## [2.1.0] - 2026-08-13

### اضافه‌شده

- **سیستم تست طلایی (Golden Tests)** برای تمام ۲۰ آزمایش با استفاده از `pytest` و فایل‌های JSON در پوشه `validation/golden_cases/`
- **موتور کمال‌گرایی** (`perfection_engine.py`) برای بهبود خودکار کیفیت فایل اکسل
- **پچ خودکار خطاهای بحرانی** (`final_golden_patch.py`) برای رفع سریع مشکلات شناسایی‌شده
- **شیت‌های مرجع کامل** شامل:
  - `_ISIRI_Limits` – حدود دانه‌بندی استاندارد ISIRI 302
  - `_Standards` – لیست کامل استانداردهای ASTM با ویرایش و وضعیت
  - `_Reference_DB` – دیتابیس مرجع پارامترهای آزمایش
- **ابزارهای CI/CD پیشرفته**:
  - `pages.yml` – انتشار خودکار صفحه GitHub Pages
  - `text-markdown-auto-fix.yml` – اصلاح خودکار فایل‌های Markdown
  - `text-markdown-qa.yml` – کیفی‌سازی محتوای متنی
- **قالب‌های Issue** (`bug_report.md` و `feature_request.md`) و **قالب Pull Request**
- **فایل‌های پیکربندی** برای کیفیت کد:
  - `.pre-commit-config.yaml` – هوک‌های پیش از commit
  - `.markdownlint.json` و `.markdownlintignore` – قوانین Markdown
  - `.prettierrc.json` و `.prettierignore` – فرمت‌کننده کد

### اصلاح‌شده

- **فرمول معادل ماسه (SE)** بر اساس ASTM D2419: اصلاح مخرج کسر از `Clay` به `Sand + Clay`
- **ارتفاع قالب اسلامپ** از ۳۰۰ به **۳۰۴.۸ میلی‌متر** (۱۲ اینچ) بر اساس ASTM C143
- **تفکیک استانداردهای خمشی**:
  - ASTM C78 برای بارگذاری یک‌سوم میانه
  - ASTM C293 برای بارگذاری مرکزی
- **نگاشت نتایج در شیت گزارش** – رفع خطاهای ارجاعی که باعث نمایش داده‌های اشتباه می‌شد
- **بررسی فیزیکی چگالی** – اضافه کردن کنترل `SSD ≥ OD` و `App ≥ SSD` در شیت‌های ۱-۳ و ۱-۴
- **Data Validation برای موبایل** – استفاده از `;` به جای `,` در لیست‌های کشویی برای سازگاری با اکسل موبایل
- **نام شیت‌ها** – اصلاح اسامی برای هماهنگی با کتاب مرجع
- **Conditional Formatting** – اعمال رنگ‌بندی پویا بر اساس مقدار سلول‌ها
- **حفاظت شیت‌ها** – فعال کردن `selectUnlockedCells` برای امکان ورود داده توسط کاربر

### تغییرات فنی

- **معماری داده‌محور** – بازنویسی کامل با استفاده از `TestSpec` و `InputField`/`OutputField`
- **مدیریت رمز از متغیر محیطی** – `WORKBOOK_PASSWORD` به‌جای هاردکد در کد
- **GitHub Actions** – ساخت و انتشار خودکار فایل اکسل با هر `push` به `main`
- **افزودن `pytest`** – اجرای تست‌های واحد و golden tests در فرآیند CI
- **افزودن `pre-commit`** – تضمین کیفیت کد قبل از هر commit
- **ساختار پوشه‌بندی بهینه** – جداسازی `src/`, `tests/`, `validation/`, `docs/`

---

## [2.0.0] - 2026-08-10

### اضافه‌شده

- **پیاده‌سازی کامل ۲۰ آزمایش** بر اساس استانداردهای ASTM و ISIRI
- **شیت اطلاعات آزمون** با فیلدهای: نام پروژه، شماره نمونه، تاریخ، اپراتور، تجهیزات، دما، رطوبت
- **شیت گزارش خودکار** با خلاصه تمام نتایج و وضعیت قبولی
- **شیت داشبورد** برای نمایش پیشرفت و وضعیت کلی
- **شیت QA Test** برای اعتبارسنجی دستی فرمول‌ها
- **نمودار دانه‌بندی** با مقیاس لگاریتمی و حدود استاندارد ISIRI 302
- **حفاظت از شیت‌ها** با رمز (قابل تغییر با متغیر محیطی)
- **تنظیمات چاپ** (A4، حاشیه، fit to page, landscape/portrait)
- **فریز کردن ردیف عنوان** در تمام شیت‌ها برای سهولت اسکرول
- **Conditional Formatting** برای وضعیت‌ها (سبز/نارنجی/قرمز)
- **پشتیبانی از زاویه چکش اشمیت** و اصلاح دما و رطوبت
- **فرمول اسلامپ** بر اساس ارتفاع واقعی قالب (۳۰۴.۸ میلی‌متر)

### اصلاح‌شده

- **فرمول معادل ماسه** از `C5/C6` به `C5/(C5+C6)` بر اساس ASTM D2419
- **تفکیک استانداردهای خمشی** – جداسازی C78 (یک‌سوم میانه) از C293 (مرکزی)
- **نگاشت نتایج در گزارش** – تصحیح آدرس سلول‌ها برای جلوگیری از نمایش اشتباه
- **فرمول‌های چگالی** – اضافه کردن بررسی فیزیکی `SSD ≥ OD` و `App ≥ SSD`
- **Data Validation لیستی** – تغییر جداکننده از `,` به `;` برای سازگاری با موبایل

### حذف‌شده

- کدهای تکراری و سخت‌کد شده – جایگزینی با معماری داده‌محور

---

## [1.1.0] - 2026-07-28

### اضافه‌شده

- شیت‌های آزمایش ۱-۸ تا ۴-۵ بر اساس فهرست کتاب
- استایل‌های مرکزی (`StyleManager`) برای یکنواختی ظاهر
- Data Validation برای تمام ورودی‌ها
- حفاظت پایه شیت‌ها
- نوار ناوبری با هایپرلینک

### اصلاح‌شده

- رفع باگ‌های فرمولی در شیت‌های ۲-۴ و ۴-۱
- اصلاح نمودار دانه‌بندی
- بهبود رنگ‌بندی

---

## [1.0.0] - 2026-07-15

### اضافه‌شده

- انتشار اولیه پروژه
- پیاده‌سازی ۱۰ آزمایش پایه
- ساختار اولیه `build.py`
- پشتیبانی از RTL (راست‌به‌چپ)
- استایل‌های اولیه

---

## راهنما

- **اضافه‌شده** – قابلیت‌های جدید
- **اصلاح‌شده** – تغییرات در قابلیت‌های موجود
- **حذف‌شده** – قابلیت‌هایی که حذف شده‌اند
- **تغییرات فنی** – تغییرات داخلی که بر کاربر تأثیر نمی‌گذارد

نسخه‌های قدیمی‌تر در مخزن موجود هستند:
[تاریخچه کامل کامیت‌ها](https://github.com/bmhmdyan279-png/Concrete-Lab-Companion/commits/main)
