# تاریخچه تغییرات (Changelog)

همه تغییرات مهم این پروژه در این فایل ثبت می‌شود.

فرمت بر اساس [Keep a Changelog](https://keepachangelog.com/fa/1.0.0/) و نسخه‌گذاری بر اساس [Semantic Versioning](https://semver.org/lang/fa/) است.

---

## [4.1.0] - 2026-09-08

نسخهٔ ۴.۱ پاسخ مستقیم به بازبینی هشت منتقد است. محور آن «صداقتِ سازگاری» است:
هر عددی که محصول را توصیف می‌کند یا از رجیستری زنده مشتق می‌شود یا با تست اجبار
می‌شود؛ به‌علاوه یک مسیر ورودی دادهٔ واقعی، تا محصول فقط گزارش نمونه نباشد.

### اضافه‌شده

- **مسیر ورودی کاربر** (`--input your_data.json` + `src/concrete_lab/cases.py`): موتور
  حالا اندازه‌گیری‌های واقعی را محاسبه می‌کند، نه فقط `DEMO_CASES`. اعتبارسنجی کامل
  (آزمایش پیاده‌سازی‌نشده، `test_id` تکراری، ساختار نادرست) با گزارش **یک‌جای همهٔ
  ایرادها** و بدون ساخت فایل در صورت رد شدن حتی یک مورد. نمونهٔ قابل‌اجرا در
  `examples/sample_input.json`.
- **مدولوس نرمی (FM) در آزمایش ۱-۱** بر اساس ASTM C136/C136M: سری الک‌های صحیح
  (۹.۵ تا ۰.۱۵ میلی‌متر، بدون الک ۰.۰۷۵ و بدون pan)، گرد کردن به ۰.۰۱، و رد صریح
  منحنی ناقص. چون C136 حد پذیرش تعیین نمی‌کند، محدودهٔ متداول ۲.۳–۳.۱ فقط به‌صورت
  **یادداشت اطلاع‌رسانی** نمایش داده می‌شود، نه وضعیت قبول/رد.
- **تبدیل جرم مانده → منحنی درصد عبوری** (`isiri_302.curve_from_retained`) همراه با
  کنترل توازن جرم ASTM C136 (۰.۳٪). ورودی آزمایش ۱-۱ حالا می‌تواند همان چیزی باشد که
  آزمایشگاه ثبت می‌کند: جرم مانده روی هر الک.
- **گرد کردن درصد عبوری به ۰.۱٪** طبق دقت گزارش C136؛ نویز ممیز شناور
  (`32.99999999999999`) دیگر وارد سلول‌ها و مقایسه‌های طلایی نمی‌شود.
- **لایهٔ تست سازگاری** (`tests/test_consistency.py`): نسخه در شش مکان، شمارش
  `implemented`/`pending` در `config.yaml`، فهرست `key_sheets` در برابر شیت‌های واقعیِ
  رندرشده، هم‌ارزی `CALCULATORS` و specها، نبودِ هش یا نسخهٔ hard-code در صفحهٔ فرود،
  نبودِ وابستگی استفاده‌نشده، نبودِ placeholder در `CITATION.cff`، docstring همهٔ
  ماژول‌ها، و ممنوعیت نوشتن فرمول اکسل در `src/`.
- **`landing/release.json` + `scripts/make_release_manifest.py`**: هویت ریلیز (نسخه،
  نام فایل، SHA-256، زمان ساخت، وضعیت QA، منبع داده) از manifest همان ساخت تولید
  می‌شود؛ حالت `--check` برای دروازهٔ CI.
- **`--config PATH` و متغیر محیطی `CONCRETE_LAB_CONFIG`** به‌همراه جست‌وجوی رو به بالا و
  **مقادیر پیش‌فرض داخل کد** (`AppConfig.defaults()`): بستهٔ نصب‌شده دیگر به وجود
  `config.yaml` در مسیر خاص وابسته نیست.
- **`scripts/audit_excel.py` بازنویسی‌شده**: ممیزی محصول در برابر قرارداد واقعی نسخهٔ ۴
  (صفر سلول فرمول، تطابق SHA-256 و manifest، موجودی شیت‌ها، بج و سلب مسئولیت
  ESTIMATION، تطابق وضعیت حفاظت، نبود متن placeholder) به‌جای سنجه‌های دوران v2/v3.
- **بستهٔ نصب‌پذیر**: `[build-system]`، `dependencies`، `optional-dependencies.dev`،
  `[project.scripts]` و کشف بسته در چیدمان `src` — یعنی `pip install .` و دستور
  `concrete-lab-companion` واقعاً کار می‌کنند.
- **`requirements-dev.txt`** برای تفکیک وابستگی زمان اجرا از ابزار تست.
- **`docs/qa/RELEASE_CHECKLIST.md` بازنویسی شد**: فهرست قبلی maintainer را گمراه می‌کرد —
  «`build.py` از ریشه حذف شود» (خلاف کل معماری v4)، «`formulas_audit.txt` به‌روز باشد»
  (یک سند تاریخی از v1.0.0)، و «هش را دستی در Release notes و صفحهٔ فرود بگذارید»
  (حالا خودکار است). فهرست جدید مشخص می‌کند کدام بند توسط CI اجبار می‌شود.
- **`tests/test_audit_script.py`**: خودِ دروازهٔ ریلیز تست دارد — از جمله برای مثبتِ
  کاذبی که نسخهٔ بازنویسی‌شده باید از آن پرهیز می‌کرد.
- **دروازهٔ lint (`ruff`) و آستانهٔ پوشش** در `pyproject.toml` و CI.
- تست‌های جدید: `test_cli_unit.py` (CLI درون‌فرآیندی؛ پوشش `cli.py` از ۲۹٪ به ۱۰۰٪)،
  `test_config.py`، `test_cases.py`، `test_golden_dispatch.py`، `test_golden_corpus.py`
  و `test_release_manifest.py`.

### تغییر‌یافته

- **`config.yaml` صادق شد**: `implemented: 20` → `7` و افزودن `pending: 13`؛
  `key_sheets` از فهرست ۳۱ شیتیِ workbook بازنشستهٔ v3 به ۱۴ شیتِ واقعیِ رندر v4
  تغییر کرد؛ `build_date` → `release_date` (زمان ساخت واقعی در manifest است).
- **پوشش سوئیت طلایی کامل شد**: موارد طلایی ۱-۱، ۴-۱ و ۴-۵ که در ۴.۰.۰ هرگز علیه موتور
  اجرا نمی‌شدند و بی‌صدا به «هشدار» تنزل یافته بودند، اکنون از مسیر کامل
  `calculate()` عبور می‌کنند. نتیجه: ۱۷ اجرای واقعی (پیش‌تر ۱۴) و ۱۴ هشدار که همگی
  **نام‌گذاری و توضیح‌دار** شده‌اند.
- **هر هشدار QA حالا یک `note` دارد** (`QAReport.notes`): شمارش هشدار بدون توضیح
  قابل اقدام نیست. این فهرست در لاگ و در شیت `24_QA_Test` چاپ می‌شود.
- **`--demo` دیگر پرچم بی‌اثر نیست**: با `--input` در یک گروه انحصاری است، پس
  `--demo --input` خطای argparse می‌دهد و منبع داده در manifest ثبت می‌شود.
- **صفحهٔ فرود**: هش، شمارهٔ نسخه، نام فایل و دستور PowerShell همه از
  `release.json` خوانده می‌شوند؛ اگر در دسترس نباشد صراحتاً «هش در دسترس نیست»
  نمایش داده می‌شود. بنر «این فایل گزارش نمونه است، نه ماشین‌حساب» از گام ۲/۳ راهنما
  به **بخش hero** منتقل شد.
- **`pyproject.toml` تنها منبع تنظیمات pytest شد** (حذف `pytest.ini`) تا هشدار
  «ignoring pytest config in pyproject.toml» و دوگانگی پیکربندی از بین برود.
- **CI**: `build.yml` یک بار می‌سازد و همان بایت‌ها را به Release و Pages می‌دهد؛
  بارگذاری ریلیز از `actions/upload-release-asset@v1` (بایگانی‌شده) به `gh` پیش‌نصب‌شده
  منتقل شد و نام فایل سخت‌کد `v4.0.0` با glob جایگزین شد. `.sha256` هم اکنون همراه
  ریلیز منتشر می‌شود. `pages.yml` حذف شد چون `build.yml` مالک یکتای استقرار Pages است.
- **`CITATION.cff` معتبر شد**: حذف placeholderهای `soon` از `version`، `doi`، `orcid` و
  `date-released`؛ افزودن `abstract`، `repository-code`، `license-url` و `keywords`.
- **`CONTRIBUTING.md` بازنویسی شد**: مارک‌آپ نامعتبر (`<div dir="rtl">` ترکیب‌شده با
  markdown، فنسِ باز، تیترهای گم‌شده) حذف و دستورهای واقعی و قابل‌اجرا جایگزین شد.
- `scripts/run-phase2.ps1` دیگر `xlsxwriter` نصب نمی‌کند.

### حذف‌شده

- **`xlsxwriter`** از وابستگی‌ها: هیچ‌جای کد استفاده نمی‌شد و رندر فقط openpyxl است.
- **`domain/results.py` (`ResultSpec`)**: کد مردهٔ بدون هیچ مصرف‌کننده، که فیلد
  **اجباری** `formula` (فرمول اکسل) داشت — نقض مستقیم معماری «بدون فرمول» نسخهٔ ۴.
- **`tests/test_golden.py`**: کد مردهٔ بازمانده از نسخهٔ ۲.۱.۰ که به workbook و
  شیت‌های موجودیت‌ندار ارجاع می‌داد و دو skip دائمی تولید می‌کرد.
- **`pytest.ini`** و **`.github/workflows/pages.yml`** (هر دو منبع دوم یک حقیقت بودند).
- **`scripts/run-phase2.ps1`**: اتوماسیون دوران v2/v3 که به `.audit/*.xlsx` (پوشه‌ای که
  وجود ندارد)، `formulas_audit.txt` (سند تاریخی v1) و نصب `xlsxwriter` ارجاع می‌داد و
  دیگر نمی‌توانست درست اجرا شود. دستورهای معادل در `CONTRIBUTING.md` آمده‌اند.
- **`docs/qa/EXCEL_AUDIT_REPORT.json`** از مخزن حذف و gitignore شد: یک گزارشِ
  _تولیدشده_ بود که مربوط به ممیزی `Concrete_Lab_Companion_v1.0.0.xlsx` می‌ماند. اکنون
  توسط `scripts/audit_excel.py` در هر ساخت تولید و به‌عنوان artifact در CI منتشر می‌شود.
- جدول عرض ستون تکراری (`assembler.DEFAULT_WIDTHS` مرده در برابر
  `renderer.DEFAULT_WIDTH_FALLBACK`) → یک ثابت در `constants.py`.
- عدد دستی «۲۱۵ تست» از README.

### اصلاح‌شده

- **باگ نصب‌پذیری: `pip install` بسته‌ای می‌داد که اجرا نمی‌شد.** `qa/engine.py` در سطح
  ماژول `pytest` را import می‌کرد، در حالی که pytest یک وابستگی _توسعه_ است؛ در نتیجه
  `concrete_lab.cli` در یک نصب ساده حتی قابل import نبود و اسکریپت کنسول با
  `ModuleNotFoundError` کرش می‌کرد. import اکنون تنبل است، نبودِ pytest با پیام
  actionable گزارش می‌شود (`QAError`)، و یک تست سازگاری اجازهٔ import سطح‌ماژولِ
  هر وابستگی dev-only را در `src/` نمی‌دهد.
- **باگ false-green دوم در سوئیت طلایی**: وقتی پکیج نصب‌شده بدون مخزن اجرا می‌شد،
  پوشهٔ `validation/golden_cases` وجود نداشت، glob هیچ فایلی پیدا نمی‌کرد و لایهٔ QA
  «۰ پاس، ۰ شکست → ✅ PASS» چاپ می‌کرد — یعنی «هیچ علمی راستی‌آزمایی نشد» به‌عنوان
  موفقیت گزارش می‌شد. اکنون لایه‌ای که هیچ چیزی را بررسی نکرده هرگز PASS نمی‌دهد:
  در `--validate` **شکست** است (کد خروج ۱) و در ساخت معمولی **هشدار بلند و مستند**،
  تا یک بستهٔ نصب‌شده همچنان بتواند workbook بسازد ولی هرگز وانمود نکند اعتبارسنجی شده.
- **باگ false-green در `QAEngine.run_pytest`**: اگر pytest پیش از اجرای تست‌ها متوقف
  می‌شد (مسیر ناموجود، آرگومان نامعتبر، خطای داخلی)، هیچ تستی شمرده نمی‌شد و وضعیت
  **PASS** گزارش می‌شد. اکنون کد خروج pytest تفسیر می‌شود و چنین اجرایی **FAIL** است.
- **باگ کرش در سوئیت طلایی**: یک فایل JSON خراب در `validation/golden_cases/` کل لایهٔ
  QA — و در نتیجه کل ساخت — را با traceback متوقف می‌کرد، برخلاف قرارداد مستندِ
  «یک لایهٔ QA نباید ساخت را کرش کند». اکنون یک شکستِ توضیح‌دار است.
- **باگ مقدار انتظاری غلط در `1-1.json`**: پروندهٔ طلایی `FM = 2.85` را انتظار
  می‌کشید که با هیچ قرائتی از همان داده‌ها سازگار نبود (مقدار درست برای آن جرم‌ها
  ۳.۷۵ است) و منحنی‌اش هم ناقص بود (۶ الک از ۸). پرونده با دادهٔ کامل و مقدار درست
  بازنویسی شد.
- **باگ ورودی ناقص در `4-1.json`**: `length_mm` نداشت، در حالی که C39 برای اصلاح L/D
  به آن نیاز دارد؛ بنابراین پرونده هرگز قابل اجرا نبود.
- **باگ علمی در `4-5.json`**: بدون هیچ همبستگی، `estimated_strength_mpa = 35` را انتظار
  می‌کشید — یعنی همان «فرمول جهانی عدد بازگشت → مقاومت» که ASTM C805 منع می‌کند و این
  پروژه صریحاً پیاده‌سازی نکرده است. اکنون همبستگی پروژه (شناسه، شیب، عرض از مبدأ)
  بخشی از ورودی است و مقدار تخمینی از آن مشتق می‌شود.
- **ناسازگاری تداخل نام ماژول در pytest تودرتو**: دو فایل تست موقت با نام یکسان باعث
  «import file mismatch» و سقوط جمع‌آوری می‌شدند؛ اجرای تودرتو اکنون
  `--import-mode=importlib` است.
- **پوشش `cli.py`**: تست‌های سرتاسری آن را به‌صورت زیرپردازش اجرا می‌کردند، پس شاخه‌های
  خودِ CLI هرگز اندازه‌گیری نمی‌شدند (۲۹٪). تست درون‌فرآیندی اضافه شد (۱۰۰٪).
- **خطای پیکربندی به‌جای traceback**: `config.yaml` نامعتبر یا ناسازگار اکنون با پیام
  `configuration rejected: …` و کد خروج ۱ گزارش می‌شود.
- `AppConfig.validate()` واقعاً اعتبارسنجی می‌کند: رنگ‌های خالی/غیررشته‌ای و نسخهٔ
  ناهمخوان با بسته رد می‌شوند.

---

## [4.0.0] - 2026-09-03

بازطراحی کامل معماری بر اساس بازبینی منتقدان: محصول از «اسکریپت تولید اکسل» به
«سیستم محاسباتی و QA مدل‌محور» تبدیل شد — علم آزمایش در پایتون، اکسل فقط نمایش.

### اضافه‌شده

- **موتور محاسباتی دامنه** (`domain/engine.py`): محاسبه ۷ آزمایش (۱-۱، ۱-۲، ۲-۱، ۳-۱، ۴-۱، ۴-۲، ۴-۵) در پایتون با اعتبارسنجی ورودی، قوانین فیزیکی و پنجره‌های عدم‌قطعیت.
- **رولسیت ASTM C39/C39M-26**: فقط استوانه (رد صریح مکعب با `OutOfScopeError`)، اصلاح L/D با جدول منتشرشده و درون‌یابی خطی، گرد کردن ۰.۱ مگاپاسکال.
- **رولسیت ASTM C805/C805M-25**: اسکوپ `ESTIMATION`، بج «ESTIMATED — NOT FOR ACCEPTANCE»، سلب مسئولیت قوی دوزبانه، قانون پرتی ۷ واحدی، و ممنوعیت فرمول جهانی — تخمین فقط با همبستگی اختصاصی پروژه.
- **رولسیت ISIRI 302 (ویرایش سوم ۱۳۹۴)**: پاکت دانه‌بندی تایپ‌شده با باند هشدار نزدیک مرز + قوانین فیزیکی منحنی، به‌جای دیکشنری ساده.
- **موتور QA** (`qa/engine.py`): اجرای واقعی pytest با گزارش ساختاریافته `QAReport` (پاس/شکست/هشدار).
- **سوئیت طلایی درون‌فرآیندی** (`qa/golden.py`): اجرای موارد طلایی مقابل موتور در هر ساخت.
- **بررسی‌های ساختاری** (`qa/structural.py`): تمامیت فایل، SHA-256، manifest و تضمین «هیچ فرمولی در اکسل نیست».
- **CLI واقعی** (`cli.py`): `--validate` حالا واقعاً QA اجرا می‌کند و در صورت شکست خروجی غیرصفر می‌دهد.
- **Manifest جدید**: `build_id`, `python_version`, `openpyxl_version`, نقشه استانداردها با ویرایش‌های حسابرسی‌شده، نسخه رولسیت‌ها، نتایج QA چندسطحی.
- **رجیستری استانداردها با حسابرسی ۲۰۲۶**: ویرایش C39 از ۲۰۲۲ به **۲۰۲۶** و C805 به **-25** به‌روز شد.
- **کاتالوگ داده‌محور ۲۰ آزمایش** (`specs/`): ثبت غیرمتمرکز فصل‌به‌فصل به‌جای تابع ۵۰۰ خطی `define_tests`.
- **مدیریت پیکربندی** (`config.py`): بارگذاری و اعتبارسنجی `config.yaml`.
- **۱۰ مورد طلایی جدید** برای سه استاندارد بازنویسی‌شده + ۵ تست‌فایل جدید (موتور، QA، رندر، manifest، CLI سرتاسری).

### تغییر‌یافته

- **`build.py` از ۱۷۰۰ خط به ارکستراتور ~۳۵ خطی** تبدیل شد.
- **رندر اکسل فقط‌نمایشی شد** (`render/excel/`): مدل داده → شیت؛ استایل‌ها با `NamedStyle` کش‌شده؛ هیچ فرمول محاسباتی نوشته نمی‌شود.
- وضعیت‌ها از ایموجی به مدل ساختاریافته `ValidationStatus` منتقل شدند؛ نمایش نماد فقط در لایه رندر.
- حفاظت شیت با لاگ صریح `Protection: ENABLED/DISABLED` و یادداشت «مرز امنیتی نیست».

### حذف‌شده

- **ضریب ۰.۹۵ خودکار برای نمونه مکعبی** در مقاومت فشاری (نقض دامنه استاندارد).
- **فرمول پله‌ای L/D** (`IF(L/D<1.8,…)`) و جایگزینی با درون‌یابی جدولی.
- فرمول‌های محاسباتی اکسل به‌عنوان محل منطق علمی.
- داربست‌های خالی (`builders/`, `models.py`, `registry.py`, `styles.py` قدیمی) و تابع خالی `_fill_demo_data` (قابلیت فانتوم `--demo` به رفتار واقعی تبدیل شد).

---

## [3.0.0] - 2026-08-22

### اضافه‌شده

- **جدول دانه‌بندی کامل** با ۸ الک استاندارد (۹.۵ تا ۰.۰۷۵ میلی‌متر)، درصد مانده، درصد عبوری، حدود ISIRI 302 و ستون «در محدوده؟».
- **نمودار دانه‌بندی** با مقیاس لگاریتمی، سری نمونه و حدود بالا/پایین ISIRI 302.
- **ضریب اصلاح L/D** در مقاومت فشاری (ASTM C39) – اعمال ضریب ۰.۹۶ برای L/D<1.8 و ۰.۹۸ برای 1.8≤L/D<1.9.
- **درصد هوای بتن** در آزمایش چگالی بتن تازه (۲-۱) بر اساس چگالی نظری و اندازه‌گیری‌شده.
- **پشتیبانی از نمونه مکعبی** در مقاومت فشاری با ضریب تبدیل ۰.۹۵.
- **ضرایب اصلاحی چکش اشمیت** برای زاویه‌های ۴۵ درجه (پایین/بالا) و اصلاح سطح مرطوب (ضریب ۱.۰۵) با منبع ACI 228.1R.
- **شیت‌های مرجع جدید**: `_ISIRI_Limits` (دقیقاً مطابق استاندارد), `_Errata` (ثبت خطاهای اصلاح‌شده), `_Validation_Data` (Golden Cases).
- **Golden Tests در شیت QA** – ۷ تست خودکار برای اعتبارسنجی فرمول‌های کلیدی (ورودی خالی، مرزی، مقادیر کتاب، SE، اسلامپ، Voids).
- **چیدمان ماتریسی خودکار** برای آزمایش‌های ۲-۴ و ۴-۵ با استفاده از `custom_builder`.
- **گزینه `--validate`** در خط فرمان برای اعتبارسنجی رجیستری بدون ساخت فایل.
- **گزینه `--no-protect`** برای غیرفعال‌سازی حفاظت شیت‌ها.
- **گزینه `--demo`** برای پر کردن داده‌های نمونه (قابل توسعه).
- **Manifest JSON** همراه با SHA-256، نسخه، تاریخ ساخت و لیست شیت‌ها.
- **قابلیت `wb.calculation.fullCalcOnLoad = True`** برای محاسبه خودکار فرمول‌ها هنگام باز شدن فایل.
- **تنظیمات چاپ** با `fitToPage=True` و هدر/فوتر شامل شماره صفحه و نسخه.

### اصلاح‌شده

- **فرمول فضای خالی (Voids) در ۱-۵** – اصلاح ارجاع از `C9` به `C10` (وزن واحد حجمی).
- **فرمول معادل ماسه (SE)** – تصحیح از `C5/(C5+C6)` به `C5/C6` طبق ASTM D2419.
- **ارتفاع قالب اسلامپ** – تغییر از ۳۰۴.۸ به ۳۰۰ میلی‌متر بر اساس ASTM C143M (نسخه متریک).
- **ضریب اصلاح سطح مرطوب چکش اشمیت** – اصلاح از ۰.۹۵ به ۱.۰۵ (چون سطح مرطوب عدد rebound را کاهش می‌دهد، ضریب باید >1 باشد).
- **چیدمان ورودی‌های چندستونه** – رفع مشکل بازنویسی برچسب‌ها و غیرقابل تایپ بودن سلول‌ها در ۲-۴ و ۴-۵.
- **اعتبارسنجی `textLength`** – پیاده‌سازی کامل برای فیلدهای اجباری شیت اطلاعات آزمون.
- **هایپرلینک‌های ناوبری** – استفاده از `Hyperlink` با `location` و کوتیشن برای نام شیت‌های فارسی.
- **نقشه گزارش** – حذف `result_map` سخت‌کد و جایگزینی با برداشت خودکار از `TEST_REGISTRY` و `summary_key`.
- **رنگ‌بندی خروجی‌ها** – حذف `style="pass"` از خروجی‌های عددی و استفاده از `style="neutral"`؛ رنگ‌بندی فقط برای وضعیت‌ها (سلول‌های شرطی).
- **فونت‌ها** – اضافه کردن فونت‌های پشتیبان (`B Nazanin, Arial, sans-serif`) برای سازگاری با سیستم‌عامل‌های غیرویندوزی.
- **جداکننده لیست‌های Data Validation** – استفاده از ویرگول (`،`) به‌جای نقطه‌ویرگول (`;`) برای سازگاری با اکسل.

### تغییرات فنی

- **تفکیک فیلدهای `dataclass`** – رعایت ترتیب (فیلدهای بدون پیش‌فرض قبل از فیلدهای با پیش‌فرض) برای جلوگیری از خطای Python.
- **بهبود Type Hints** و رفع هشدارهای IDE (importهای بی‌استفاده، ارجاع به `Series`، خصوصیات `chart.scaling`).
- **مدیریت خطای باز بودن فایل** – پیام واضح به فارسی و خروج با کد ۱.
- **ساختار ماژولار** – امکان اضافه کردن `custom_builder` برای شیت‌های خاص بدون تغییر کد اصلی.
- **ثبت خطاهای اصلاح‌شده** در شیت `_Errata` برای شفافیت و مستندسازی.

### حذف‌شده

- `result_map` سخت‌کد شده – جایگزین با نقشه خودکار.
- `style="pass"` از خروجی‌های عددی – رنگ‌بندی به سلول‌های شرطی واگذار شد.
- کدهای مرده و متغیرهای استفاده‌نشده.

---

## [2.1.0] - 2026-08-13

### اضافه‌شده

- **سیستم تست طلایی (Golden Tests)** برای تمام ۲۰ آزمایش با استفاده از `pytest` و فایل‌های JSON در پوشه `validation/golden_cases/`
- **موتور کمال‌گرایی** (`perfection_engine.py`) برای بهبود خودکار کیفیت فایل اکسل
- **پچ خودکار خطاهای بحرانی** (`final_golden_patch.py`) برای رفع سریع مشکلات شناسایی‌شده
- **شیت‌های مرجع کامل** شامل:
  - `_ISIRI_Limits` – حدود دانه‌بندی استاندارد ISIRI 302
  - `_Standards` – لیست کامل استانداردهای ASTM با ویرایش و وضعیت
  - `_Reference_DB` – دیتابیس مرجع پارامترهای آزمایش
- **ابزارهای CI/CD پیشرفته**:
  - `pages.yml` – انتشار خودکار صفحه GitHub Pages
  - `text-markdown-auto-fix.yml` – اصلاح خودکار فایل‌های Markdown
  - `text-markdown-qa.yml` – کیفی‌سازی محتوای متنی
- **قالب‌های Issue** (`bug_report.md` و `feature_request.md`) و **قالب Pull Request**
- **فایل‌های پیکربندی** برای کیفیت کد:
  - `.pre-commit-config.yaml` – هوک‌های پیش از commit
  - `.markdownlint.json` و `.markdownlintignore` – قوانین Markdown
  - `.prettierrc.json` و `.prettierignore` – فرمت‌کننده کد

### اصلاح‌شده

- **فرمول معادل ماسه (SE)** بر اساس ASTM D2419: اصلاح مخرج کسر از `Clay` به `Sand + Clay`
- **ارتفاع قالب اسلامپ** از ۳۰۰ به **۳۰۴.۸ میلی‌متر** (۱۲ اینچ) بر اساس ASTM C143
- **تفکیک استانداردهای خمشی**:
  - ASTM C78 برای بارگذاری یک‌سوم میانه
  - ASTM C293 برای بارگذاری مرکزی
- **نگاشت نتایج در شیت گزارش** – رفع خطاهای ارجاعی که باعث نمایش داده‌های اشتباه می‌شد
- **بررسی فیزیکی چگالی** – اضافه کردن کنترل `SSD ≥ OD` و `App ≥ SSD` در شیت‌های ۱-۳ و ۱-۴
- **Data Validation برای موبایل** – استفاده از `;` به جای `,` در لیست‌های کشویی برای سازگاری با اکسل موبایل
- **نام شیت‌ها** – اصلاح اسامی برای هماهنگی با کتاب مرجع
- **Conditional Formatting** – اعمال رنگ‌بندی پویا بر اساس مقدار سلول‌ها
- **حفاظت شیت‌ها** – فعال کردن `selectUnlockedCells` برای امکان ورود داده توسط کاربر

### تغییرات فنی

- **معماری داده‌محور** – بازنویسی کامل با استفاده از `TestSpec` و `InputField`/`OutputField`
- **مدیریت رمز از متغیر محیطی** – `WORKBOOK_PASSWORD` به‌جای هاردکد در کد
- **GitHub Actions** – ساخت و انتشار خودکار فایل اکسل با هر `push` به `main`
- **افزودن `pytest`** – اجرای تست‌های واحد و golden tests در فرآیند CI
- **افزودن `pre-commit`** – تضمین کیفیت کد قبل از هر commit
- **ساختار پوشه‌بندی بهینه** – جداسازی `src/`, `tests/`, `validation/`, `docs/`

---

## [2.0.0] - 2026-08-10

### اضافه‌شده

- **پیاده‌سازی کامل ۲۰ آزمایش** بر اساس استانداردهای ASTM و ISIRI
- **شیت اطلاعات آزمون** با فیلدهای: نام پروژه، شماره نمونه، تاریخ، اپراتور، تجهیزات، دما، رطوبت
- **شیت گزارش خودکار** با خلاصه تمام نتایج و وضعیت قبولی
- **شیت داشبورد** برای نمایش پیشرفت و وضعیت کلی
- **شیت QA Test** برای اعتبارسنجی دستی فرمول‌ها
- **نمودار دانه‌بندی** با مقیاس لگاریتمی و حدود استاندارد ISIRI 302
- **حفاظت از شیت‌ها** با رمز (قابل تغییر با متغیر محیطی)
- **تنظیمات چاپ** (A4، حاشیه، fit to page, landscape/portrait)
- **فریز کردن ردیف عنوان** در تمام شیت‌ها برای سهولت اسکرول
- **Conditional Formatting** برای وضعیت‌ها (سبز/نارنجی/قرمز)
- **پشتیبانی از زاویه چکش اشمیت** و اصلاح دما و رطوبت
- **فرمول اسلامپ** بر اساس ارتفاع واقعی قالب (۳۰۴.۸ میلی‌متر)

### اصلاح‌شده

- **فرمول معادل ماسه** از `C5/C6` به `C5/(C5+C6)` بر اساس ASTM D2419
- **تفکیک استانداردهای خمشی** – جداسازی C78 (یک‌سوم میانه) از C293 (مرکزی)
- **نگاشت نتایج در گزارش** – تصحیح آدرس سلول‌ها برای جلوگیری از نمایش اشتباه
- **فرمول‌های چگالی** – اضافه کردن بررسی فیزیکی `SSD ≥ OD` و `App ≥ SSD`
- **Data Validation لیستی** – تغییر جداکننده از `,` به `;` برای سازگاری با موبایل

### حذف‌شده

- کدهای تکراری و سخت‌کد شده – جایگزینی با معماری داده‌محور

---

## [1.1.0] - 2026-07-28

### اضافه‌شده

- شیت‌های آزمایش ۱-۸ تا ۴-۵ بر اساس فهرست کتاب
- استایل‌های مرکزی (`StyleManager`) برای یکنواختی ظاهر
- Data Validation برای تمام ورودی‌ها
- حفاظت پایه شیت‌ها
- نوار ناوبری با هایپرلینک

### اصلاح‌شده

- رفع باگ‌های فرمولی در شیت‌های ۲-۴ و ۴-۱
- اصلاح نمودار دانه‌بندی
- بهبود رنگ‌بندی

---

## [1.0.0] - 2026-07-15

### اضافه‌شده

- انتشار اولیه پروژه
- پیاده‌سازی ۱۰ آزمایش پایه
- ساختار اولیه `build.py`
- پشتیبانی از RTL (راست‌به‌چپ)
- استایل‌های اولیه

---

## راهنما

- **اضافه‌شده** – قابلیت‌های جدید
- **اصلاح‌شده** – تغییرات در قابلیت‌های موجود
- **حذف‌شده** – قابلیت‌هایی که حذف شده‌اند
- **تغییرات فنی** – تغییرات داخلی که بر کاربر تأثیر نمی‌گذارد

نسخه‌های قدیمی‌تر در مخزن موجود هستند:
[تاریخچه کامل کامیت‌ها](https://github.com/bmhmdyan279-png/Concrete-Lab-Companion/commits/main)
