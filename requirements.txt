# Runtime dependencies — everything needed to build the product.
#
# Test and lint tooling lives in requirements-dev.txt.  Both lists are
# cross-checked against pyproject.toml by tests/test_consistency.py, and
# every entry here must actually be imported by src/concrete_lab
# (xlsxwriter sat in this file unused until 4.1.0).
#
# Install for development with:  pip install -r requirements-dev.txt
openpyxl>=3.1,<4
pyyaml>=6.0,<7
